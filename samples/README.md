# Sample data

- **sample.pdf**: Generate with `python scripts/create_sample_pdf.py` (optional: `pip install fpdf2` for PDF with extractable text).
- **City of Sample**: Use jurisdiction name `City of Sample` for the sample adapter (checklist, portal research, contacts).
