# Permitting Agent - optional env (CLI can use flags instead)
# Flask session (required for multi-step flows in production)
# SECRET_KEY=your-secret-key-here

# Portal credentials - only used if user supplies; never scrape behind login without these
# PORTAL_USER=
# PORTAL_PASSWORD=

# Output directories (defaults: ./output, ./data)
# OUTPUT_DIR=./output
# DATA_DIR=./data

# Rate limit: requests per second for portal research
# RATE_LIMIT_RPS=1.0

# Log level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO
